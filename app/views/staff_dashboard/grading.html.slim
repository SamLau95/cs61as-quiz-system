h3 Quizzes To Grade


- if @grade.empty?
  p You don't have any quizzes to grade!
- else
  ul
    - @grade.each do |g|
      li
        = link_to g, view_quiz_path(g.student_id, 
                                    quiz_id: g.quiz_id)