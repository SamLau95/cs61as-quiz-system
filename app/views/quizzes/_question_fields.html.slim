.row
  .small-12.columns
    h3 Question #{question.number} (#{question.points} points)
    = label_tag "submissions[#{question.number}]", question.content
    = text_field_tag "submissions[#{question.number}]", '', id: question.id
    br
    hr